name: 🐶ondogdog

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1

      - name: install
        run: |
          ./install.sh

      - uses: chiyang10000/ondogdog@test
